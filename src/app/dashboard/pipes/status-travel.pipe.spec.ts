import { StatusTravelPipe } from './status-travel.pipe';

describe('StatusTravelPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTravelPipe();
    expect(pipe).toBeTruthy();
  });
});
